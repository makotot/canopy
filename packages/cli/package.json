{
  "name": "@makotot/canopy-cli",
  "version": "0.10.0",
  "description": "CLI for Canopy — visualize React render trees as Mermaid flowcharts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/makotot/canopy.git",
    "directory": "packages/cli"
  },
  "type": "module",
  "bin": {
    "canopy": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "@makotot/canopy-annotator-async": "workspace:*",
    "@makotot/canopy-annotator-client-boundary": "workspace:*",
    "@makotot/canopy-annotator-suspense": "workspace:*",
    "@makotot/canopy-annotator-context": "workspace:*",
    "@makotot/canopy-annotator-external": "workspace:*",
    "@makotot/canopy-core": "workspace:*",
    "@makotot/canopy-reporter-json": "workspace:*",
    "@makotot/canopy-reporter-mermaid": "workspace:*",
    "@makotot/canopy-reporter-tree": "workspace:*",
    "cac": "7.0.0"
  },
  "devDependencies": {
    "ts-morph": "27.0.2"
  }
}

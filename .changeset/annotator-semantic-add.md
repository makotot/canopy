---
'@makotot/canopy-annotator-semantic': minor
---

Add `@makotot/canopy-annotator-semantic` — annotates HTML elements with their ARIA implicit role as `meta.badge` (e.g. `header` → `banner`, `h1` → `heading lv1`, `div` → `generic`), making the render tree diagram reflect the accessibility tree structure.

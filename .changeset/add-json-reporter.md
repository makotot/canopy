---
'@makotot/canopy-reporter-json': minor
'@makotot/canopy-cli': minor
---

Add JSON reporter. `@makotot/canopy-reporter-json` is a new package that serializes the render tree as JSON. The CLI gains a `--reporter` option (e.g. `--reporter json`) to select the reporter at runtime; interactive mode also exposes this choice.

---
'@makotot/canopy-cli': minor
---

Add -i / --interactive flag to the canopy command. When specified, the CLI prompts for the entry point file, component name, and annotators interactively instead of requiring them as arguments.